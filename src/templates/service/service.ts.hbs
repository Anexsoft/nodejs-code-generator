import { {{domain}} } from '{{refs.domain}}/{{lowercase domain}}.domain';
import { {{domain}}Repository } from '{{refs.repository}}/{{lowercase domain}}.repository';

export class {{domain}}Service {
  constructor(private readonly {{lowercase domain}}Repository: {{domain}}Repository) {
    this.{{lowercase domain}}Repository = {{lowercase domain}}Repository;
  }

  findAll(take: number, skip: number): {{domain}}[] {
    return this.{{lowercase domain}}Repository.findAll(take, skip);
  }

  findbyId(id): {{domain}} {
    return this.{{lowercase domain}}Repository.findbyId(id);
  }

  create(entry) {
    return this.{{lowercase domain}}Repository.create(entry);
  }

  update(id, entry) {
    return this.{{lowercase domain}}Repository.update(id, entry);
  }

  remove(id) {
    return this.{{lowercase domain}}Repository.remove(id);
  }
}
