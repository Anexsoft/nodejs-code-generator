export class {{entity}}Service {
  constructor({{lowercase entity}}Repository) {
    this.{{lowercase entity}}Repository = {{lowercase entity}}Repository;
  }

  findAll(take, skip) {
    return this.{{lowercase entity}}Repository.findAll(take, skip);
  }

  findbyId(id) {
    return this.{{lowercase entity}}Repository.findbyId(id);
  }

  create(entry) {
    return this.{{lowercase entity}}Repository.create(entry);
  }

  update(id, entry) {
    return this.{{lowercase entity}}Repository.update(id, entry);
  }

  remove(id) {
    return this.{{lowercase entity}}Repository.remove(id);
  }
}
